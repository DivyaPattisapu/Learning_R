n<-1000
(rollno <- 1:n)
data.frame(rollno)
(sname=paste('Student',1:n,Sep=''))
(gender=sample(x=c('M','F'),1000,replace=TRUE))
(age=runif(n,min=20,max=30)
  age <- round(age,0)
  age
(marks1=rnorm(n,mean=65,sd=10))
  (marks1=rnorm(n,mean=70,sd=8))
  